# proxy-closer
Checks for specific elements. If those elements exist, overwrites the page's HTML.

Originally created to block the Rosin proxy sitest the kids are using. GoGuardian doesn't ever seem to block it.
